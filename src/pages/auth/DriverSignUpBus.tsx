import { View, Text } from 'react-native';
export default function DriverSignUpBusScreen() {
  return <View style={{ flex:1, alignItems:'center', justifyContent:'center' }}><Text>Driver Sign Up — Bus</Text></View>;
}