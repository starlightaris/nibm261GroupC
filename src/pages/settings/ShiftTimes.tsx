import { View, Text } from 'react-native';
export default function ShiftTimesScreen() {
  return <View style={{ flex:1, alignItems:'center', justifyContent:'center' }}><Text>Shift Times</Text></View>;
}